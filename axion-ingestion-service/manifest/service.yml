apiVersion: v1
kind: Service

metadata:
  name: axion-ingestion-service

spec:
  type: ClusterIP

  selector:
    app: axion-ingestion-pod

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000