apiVersion: v1
kind: Service

metadata:
  name: axion-telemetry-query-service

spec:
  type: ClusterIP

  selector:
    app: axion-telemetry-query-service-pod

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000